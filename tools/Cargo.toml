[package]
name = "chewing-cli"
description = "Tools of the Chewing (酷音) intelligent Zhuyin input method."
version.workspace = true
license.workspace = true
rust-version.workspace = true
edition.workspace = true

[dependencies]
anyhow = "1.0.0"
chewing = { workspace = true }
clap = { version = "4.4.18", features = ["derive"] }
# Switch to mpmc_channel once it stablizes
# https://github.com/rust-lang/rust/pull/158729
# https://github.com/rust-lang/rust/issues/126840
crossbeam-channel = "0.5.16"
env_logger = { workspace = true }
scoped-error = "0.1.5"

[dependencies.clap_mangen]
version = "0.2.12"
optional = true

[features]
mangen = ["dep:clap_mangen"]
sqlite = []
sqlite-bundled = []
